services:

  gerencia:
    build:
      context: ../
      dockerfile: deployments/dockerfile.gerencia
    ports:
      - "5000:5000"

  gravacao:
    build:
      context: ../
      dockerfile: deployments/dockerfile.gravacao
    ports:
      - "5001:5001"
    links:
      - gerencia
