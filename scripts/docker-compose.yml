services:

  gerencia:
    build:
      context: ../
      dockerfile: build/dockerfile.gerencia
    ports:
      - "5000:5000"

  gravacao:
    build:
      context: ../
      dockerfile: build/dockerfile.gravacao
    ports:
      - "5001:5001"
    links:
      - gerencia
